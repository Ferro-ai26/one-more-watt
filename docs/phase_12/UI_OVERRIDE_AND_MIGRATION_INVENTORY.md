# UI Override and Migration Inventory

## Phase 12 migrated scope

| Surface | Before | Phase 12 result |
| --- | --- | --- |
| `Main.tscn` shell/background | Inline raw colors and shell StyleBox | Live Theme plus semantic token application |
| Main Grid composition | Compact dashboard with text-only environment badge | Environment-first live Desk diorama, attached ivory request plate, operator authorization, metrics, routing, navigation |
| Build cards | Inline navy StyleBox and per-label colors/sizes | `ShopItemCard` semantic styles and explicit state API |
| Grid metrics | Inline navy StyleBox and raw limiting colors | `VitalCard` semantic styles, mono numeric role, text-reinforced limiting state |
| WATT/environment | Text glyph only | Original glyph remains accessible; scratched code-native core is physically present in the live environment |
| Fonts | Engine fallback | Packaged Atkinson Hyperlegible Next/Mono with OFL provenance |

The migrated runtime scope has no raw `Color(...)` literals outside `SkinTokens` and the serialized Theme resource. Screenshot evidence is stored under `docs/phase_12/evidence/`.

## Preserved behavior

The existing request/purchase signals, node automation names, four-tab navigation, settings hooks, text scaling, reduced motion, safe-area structure, save schema, balance formulas, deterministic simulation, and Android configuration are unchanged. The environment consumes duplicated display counts and never mutates domain state.

## Remaining migration inventory

These are deliberately not completed in Phase 12:

| Owner phase | Remaining work | Current safe fallback |
| --- | --- | --- |
| Phase 13 | Manually reconstruct final Era 1 painted workshop layers, WATT production sprite/animation, infrastructure clusters, authored installation states, and production icons | Code-native Desk environment and scratched core |
| Phase 13 | Convert request/report/settings/purchase overlays into dedicated reusable themed scene components | Existing dynamic controls use the new global Theme/tokens |
| Phase 13 | Replace the retained `base_theme.tres` legacy fallback after every dependent scene is confirmed migrated | File remains preserved and unused by `Main.tscn` |
| Phase 13 | Add production audio for skin-specific interactions if authorized by its contract | Existing procedural feedback remains functional |
| Phase 14 | Physical-device visual, touch, safe-area, larger-text, reduced-motion, lifecycle, and performance review | Five host portrait layouts and graphical baselines |
| Later era phases | Author production environments, WATT distributed appearances, infrastructure clusters, and takeover consequences per era | Base-plus-variant environment contract |

No Phase 13 asset, full Eras 1–3 skin, Era 4 content, or generated concept background was introduced.
